namespace AltiumTestTask
{
    public static class Constants
    {
        public const string SEPARATOR = ". ";
        public const int MIN_STRING_LENGTH = 5;
        public const int MAX_STRING_LENGTH = 1024;
        public const double DUPLICATE_STRING_CHANCE = 0.02; // 2%
        public const int DUPLICATE_STRINGS_BAG_SIZE = 5; // keep so many to be used as duplicates
    }
}